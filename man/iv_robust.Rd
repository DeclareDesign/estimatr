% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/iv_robust.R
\name{iv_robust}
\alias{iv_robust}
\title{Two-Stage Least Squares Instrumental Variables Regression}
\usage{
iv_robust(
  formula,
  data,
  weights,
  subset,
  clusters,
  fixed_effects,
  se_type = NULL,
  ci = TRUE,
  alpha = 0.05,
  diagnostics = FALSE,
  return_vcov = TRUE,
  try_cholesky = FALSE
)
}
\arguments{
\item{formula}{(required) An object of class formula with regressors and instruments,
e.g. \code{y ~ x1 + x2 | z1 + z2}.}

\item{data}{(optional) A \code{data.frame}}

\item{weights}{(optional) The bare (unquoted) name of the weights variable}

\item{subset}{(optional) A bare (unquoted) expression specifying a subset}

\item{clusters}{(optional) A bare (unquoted) name of the cluster variable}

\item{fixed_effects}{(optional) A one-sided formula of fixed effects to absorb,
such as \code{~ blockID}. Uses FWL demeaning (see \code{\link[=lm_robust]{lm_robust()}} for details and
SE type restrictions). Diagnostics are not available with \code{fixed_effects}.}

\item{se_type}{(optional) The standard error type. \code{"HC2"} and \code{"HC3"} work with
\code{fixed_effects} at any number of factors: the second stage runs on fitted
regressors, but those are demeaned by the same fixed effects, so the
leverage decomposition \code{\link[=lm_robust]{lm_robust()}} describes applies unchanged. \code{"CR2"}
with \code{fixed_effects} expands the dummies, as in estimatr 1.0.6.
Defaults: \code{"HC2"} (no clusters, with or without FE), \code{"CR2"} (clusters, no
FE), \code{"CR0"} (clusters, with FE).}

\item{ci}{(optional) Logical. Whether to compute p-values and confidence intervals.}

\item{alpha}{(optional) The significance level, 0.05 by default.}

\item{diagnostics}{(optional) Logical. Whether to compute IV diagnostic statistics.}

\item{return_vcov}{(optional) Logical. Whether to return the vcov matrix.}

\item{try_cholesky}{(optional) Logical. Whether to solve by Cholesky
decomposition of \verb{X'X} rather than by the default pivoted QR. \code{FALSE} by
default, and worth turning on in most applied settings: about 1.4 times
faster at n = 100,000 with two regressors, and 1.7 times faster at
n = 200,000 with 60 regressors, where it is 0.15s against 0.25s. The
saving is per fit, so it is worth most in a simulation that fits the same
design thousands of times.

Rank deficiency is caught on either path. Redundant columns come back as
\code{NA} exactly as they do from \code{\link[=lm]{lm()}} whichever path ran, and a design that
is rank deficient falls back to the QR.

Whether it is safe turns on one question, whether two regressors are
nearly the same variable. Forming \verb{X'X} squares the condition number, so
the Cholesky path has about twice the rounding error of the QR, and
only near-collinearity makes that visible. Differences of scale do not,
because the columns are normalized before either decomposition, so a
covariate in dollars beside one in years costs nothing. For a treatment
indicator, a few covariates, block or cluster dummies, the centered
interactions \code{\link[=lm_lin]{lm_lin()}} builds, or a factorial, the two paths agree to at
least 10 significant digits, which is why \code{\link[=difference_in_means]{difference_in_means()}} sets it
to \code{TRUE} internally. Agreement falls to about 3 digits as the scaled
condition index reaches \code{1e6}, and the QR fallback takes over above
roughly \code{1e8}. Nothing interpretable lives in that range: a design at
\code{1e6} returns a coefficient of 4.8e4 with a standard error of 4.6e4 on a
regressor whose true effect is zero. To check a design directly, scale the
columns first, since the unscaled condition number of a design in mixed
units is large for a reason that does not affect the fit:
\code{kappa(sweep(X, 2, sqrt(colSums(X^2)), "/"), exact = TRUE)}.}
}
\value{
An object of class \code{"iv_robust"}, a list holding the estimate table in \code{coefficients}, \code{std.error}, \code{df}, \code{statistic},
\code{p.value}, \code{conf.low}, \code{conf.high}, \code{term}, and \code{outcome}; the fit in
\code{fitted.values}, \code{residuals}, \code{vcov}, \code{nobs}, \code{k}, \code{rank}, \code{df.residual},
and \code{res_var}; the summary statistics \code{r.squared}, \code{adj.r.squared},
\code{tss}, and \code{fstatistic}; and \code{se_type}, \code{weighted}, \code{clustered}, \code{fes},
\code{alpha}, \code{terms}, \code{xlevels}, and \code{call}.

\code{residuals} are the structural residuals, \verb{y - X beta}, rather than the
second-stage ones. \code{ei.iv}, \code{terms_regressors}, and \code{formula} record the
two-stage structure. With \code{diagnostics = TRUE} the object also holds
\code{diagnostic_first_stage_fstatistic}, \code{diagnostic_endogeneity_test}, and
\code{diagnostic_overid_test}.
}
\description{
Fits a two-stage least squares instrumental variables regression and
returns heteroskedasticity-robust or cluster-robust standard errors, with
optional weak-instrument, Wu-Hausman, and Sargan diagnostics.
}
\examples{
set.seed(25)
n <- 200
dat <- data.frame(z = rbinom(n, 1, 0.5), cl = rep(1:20, each = 10))
dat$x <- dat$z * rbinom(n, 1, 0.7)
dat$y <- dat$x + rnorm(n)

# Endogenous regressor on the left of the bar, instrument on the right
fit <- iv_robust(y ~ x | z, data = dat)
tidy(fit)

# The same variance menu as lm_robust()
iv_robust(y ~ x | z, data = dat, se_type = "classical")
iv_robust(y ~ x | z, data = dat, clusters = cl)

# Weak-instrument, endogeneity, and overidentification tests
summary(iv_robust(y ~ x | z, data = dat, diagnostics = TRUE))

}
