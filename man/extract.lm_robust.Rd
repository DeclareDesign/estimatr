% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/S3_extract.R
\name{extract.lm_robust}
\alias{extract.lm_robust}
\alias{extract.iv_robust}
\title{Extract model data for the \pkg{texreg} package}
\usage{
extract.lm_robust(
  model,
  include.ci = TRUE,
  include.rsquared = TRUE,
  include.adjrs = TRUE,
  include.nobs = TRUE,
  include.fstatistic = FALSE,
  include.rmse = TRUE,
  include.nclusts = TRUE,
  ...
)

extract.iv_robust(
  model,
  include.ci = TRUE,
  include.rsquared = TRUE,
  include.adjrs = TRUE,
  include.nobs = TRUE,
  include.fstatistic = FALSE,
  include.rmse = TRUE,
  include.nclusts = TRUE,
  ...
)
}
\arguments{
\item{model}{An \code{lm_robust} or \code{iv_robust} fit.}

\item{include.ci, include.rsquared, include.adjrs, include.nobs}{Logical.}

\item{include.fstatistic, include.rmse, include.nclusts}{Logical.}

\item{...}{Unused.}
}
\value{
A \pkg{texreg} object.
}
\description{
Prepares an \code{lm_robust} or \code{iv_robust} fit for \pkg{texreg}. Largely a clone
of texreg's own \code{extract.lm} method.
}
\details{
These are exported as plain functions rather than registered with
\code{S3method()} because that is how \pkg{texreg} finds them: it looks up
\verb{extract.<class>} by name in the package namespace rather than dispatching on
a generic it owns. Registering them the usual way would leave texreg unable
to see them.

\pkg{texreg} is the only consumer. Table building through
\pkg{modelsummary} needs nothing here, since it reads \code{\link[=tidy]{tidy()}} and
\code{\link[=glance]{glance()}} and so already works on every estimator in this package.
}
