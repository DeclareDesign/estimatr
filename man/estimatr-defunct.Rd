% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/deprecated.R
\name{estimatr-defunct}
\alias{estimatr-defunct}
\alias{commarobust}
\alias{starprep}
\title{Functions removed in 2.0}
\usage{
commarobust(...)

starprep(...)
}
\arguments{
\item{...}{Ignored.}
}
\value{
Never returns; both functions signal an error.
}
\description{
\code{commarobust()} and \code{starprep()} were helpers for producing robust standard
errors outside the package's own estimators and formatting them for
\pkg{stargazer}. Both are removed.
}
\details{
They are kept here as names that error rather than deleted outright, so that
a script written against estimatr 1.x says what happened and what to do
instead of failing with \verb{could not find function}.

\code{commarobust()} recomputed robust standard errors on a fitted \code{lm}. Fit the
model with \code{\link[=lm_robust]{lm_robust()}} instead, which is what it was reimplementing.

\code{starprep()} prepared a list of fits for \pkg{stargazer}, which has not been
maintained for years. Table-building now goes through
\pkg{modelsummary}, which reads \code{\link[=tidy]{tidy()}} and \code{\link[=glance]{glance()}} and therefore
works on every estimator in this package without any adapter.
}
\examples{
# Both of these error. The replacements:
set.seed(1)
dat <- data.frame(y = rnorm(20), z = rep(0:1, 10))

# was: commarobust(lm(y ~ z, data = dat))
lm_robust(y ~ z, data = dat)

# was: starprep(fit1, fit2) |> stargazer::stargazer()
# now: modelsummary::modelsummary(list(fit1, fit2))
}
