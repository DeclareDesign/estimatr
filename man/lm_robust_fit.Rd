% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helper_fit.R
\name{lm_robust_fit}
\alias{lm_robust_fit}
\title{Internal method that creates linear fits}
\usage{
lm_robust_fit(
  y,
  X,
  weights,
  cluster,
  ci = TRUE,
  se_type,
  has_int,
  alpha = 0.05,
  return_vcov = TRUE,
  return_fit = TRUE,
  try_cholesky = FALSE,
  iv_stage = list(0),
  fe_rank = 0L,
  fe_leverage = NULL,
  femat = NULL,
  linear_hypothesis = NULL
)
}
\arguments{
\item{y}{numeric outcome vector or matrix}

\item{X}{numeric design matrix}

\item{weights}{numeric weights vector}

\item{cluster}{numeric cluster vector}

\item{ci}{boolean, whether to return confidence intervals and p-values}

\item{se_type}{character denoting which kind of SEs to return}

\item{has_int}{logical, whether the model has an intercept}

\item{alpha}{numeric, test size for confidence intervals}

\item{return_vcov}{logical, whether to return the vcov matrix}

\item{return_fit}{logical, whether to return fitted values}

\item{try_cholesky}{logical. Solve by Cholesky decomposition of \verb{X'X}
rather than by the pivoted QR, falling back to the QR where the design is
rank deficient. See \code{\link[=lm_robust]{lm_robust()}} for when the fast path is safe.}

\item{iv_stage}{list of length one or two for 2SLS stages}

\item{fe_rank}{integer, degrees of freedom absorbed by fixed effects}

\item{fe_leverage}{numeric vector of per-observation leverage contributed by
the absorbed fixed effects, or \code{NULL}. \code{h_ii} of the full design splits
exactly into the demeaned-X leverage plus this term, for any number of FE
factors, which is what makes HC2 and HC3 available under \code{fixed_effects}
without building the dummy matrix.}

\item{femat}{optional numeric matrix of fixed-effect dummies for the
estimation sample. Only \code{CR2} needs it: its adjustment is built from
cluster-level blocks of the hat matrix, which do not decompose the way the
diagonal does. HC2 and HC3 take \code{fe_leverage} instead. \code{NULL} unless the
requested \code{se_type} requires it.}

\item{linear_hypothesis}{optional hypotheses, in the form \code{\link[=lh_robust]{lh_robust()}}
takes, whose \code{CR2} Satterthwaite degrees of freedom are returned as
\code{hypothesis_df}. Ignored for every other \code{se_type}, where a combination of
coefficients has the same degrees of freedom as each of them.}
}
\description{
Internal method that creates linear fits
}
\examples{
# The fitter behind lm_robust(), exported for packages that have already
# built their own design matrix. Most users want lm_robust().
set.seed(45)
X <- cbind(`(Intercept)` = 1, x = rnorm(50))
y <- X[, "x"] + rnorm(50)

lm_robust_fit(
  y = y, X = X,
  weights = NULL, cluster = NULL,
  se_type = "HC2", has_int = TRUE
)

}
